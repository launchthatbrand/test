apiVersion: v1 
kind: Service
metadata:
  name: "$Username-$random-docker-wine-$product-$i"
  labels:
    app: "$Username-$random-docker-wine-$product-$i"
    client: "$Username-$random-$i"
    user: "$Username"
spec:
  ports:
  - port: 8080
    protocol: TCP
    name: "$Username-$random-novnc"
  - port: 5900
    protocol: TCP
    name: "$Username-$random-vnc"
    # nodePort : 30380
  selector:
    app: "$Username-$random-docker-wine-$product-$i"
    client: "$Username-$random-$i"
    user: "$Username"
  type: NodePort
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "$Username-docker-wine-$product-$i"
  labels:
    app: "$Username-$random-docker-wine-$product-$i"
    client: "$Username-$random-$i"
    user: "$Username"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
